config {
  type: "view",
  schema: "warehouse_staging"
}

SELECT
  CAST(payment_id AS INT64) AS payment_id,
  CAST(order_id AS INT64) AS order_id,
  UPPER(TRIM(payment_status)) AS payment_status,
  CAST(amount AS NUMERIC) AS amount,
  paid_at
FROM ${ref("public_payments")}