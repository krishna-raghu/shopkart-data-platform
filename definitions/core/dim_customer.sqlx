config {
  type: "table",
  schema: "warehouse_core"
}

SELECT
  customer_id,
  CONCAT(first_name, ' ', last_name) AS customer_name,
  email,
  city,
  created_at,
  updated_at
FROM ${ref("stg_customers")}